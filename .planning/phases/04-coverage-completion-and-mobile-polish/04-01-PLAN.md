---
phase: 04-coverage-completion-and-mobile-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - seed_topics.py
  - app/templates/fragments/explainer_content.html
  - app/templates/fragments/chat_panel.html
  - app/templates/topic.html
autonomous: true
requirements:
  - CONT-01
  - CONT-02

must_haves:
  truths:
    - "User can browse Biology, Chemistry, and Fisica e Matematica subject groups on the home page"
    - "mitocondri topic exists as a browsable Biology topic (fixing the orphaned quiz questions)"
    - "Anatomy/physiology biology topics appear in the Biology group (tessuti animali, sistemi organo, etc.)"
    - "Physics and Math topics appear in the Fisica e Matematica group (at least 8 topics)"
    - "Explainer body text is at least 16px on iPhone Safari (no zoom required to read)"
    - "Language toggle IT/EN buttons have a minimum 44px tap target on iPhone"
    - "Chat input field has a minimum 44px tap target on iPhone"
  artifacts:
    - path: "seed_topics.py"
      provides: "Extended TOPICS list with mitocondri, anatomy/physiology biology, extra chemistry, and all physics/math entries"
      contains: "subject.*physics"
    - path: "app/templates/fragments/explainer_content.html"
      provides: "Body text at 1rem minimum via inline style override"
      contains: "font-size: 1rem"
    - path: "app/templates/fragments/chat_panel.html"
      provides: "Chat input with min-height: 44px"
      contains: "min-height: 44px"
    - path: "app/templates/topic.html"
      provides: "Language toggle buttons with inline min-height: 44px (not purged Tailwind class)"
      contains: "style=\"min-height: 44px\""
  key_links:
    - from: "seed_topics.py TOPICS list"
      to: "topics table in osteoprep.db"
      via: "INSERT OR IGNORE via sqlite3"
      pattern: "INSERT OR IGNORE INTO topics"
    - from: "topics.subject = 'physics'"
      to: "index.html Fisica e Matematica accordion"
      via: "subject group mapping already in template"
      pattern: "physics.*Fisica e Matematica"
---

<objective>
Seed all missing topics into the database and fix three identified iPhone Safari mobile polish issues.

Purpose: Phase 4 has two parallel work streams — (1) extend topic coverage to the full MUR exam syllabus and (2) fix known CSS tap-target and font-size issues on iPhone Safari. This plan executes both streams together since they touch different files with no conflict.

Output: Extended seed_topics.py with 20+ new entries (mitocondri fix, anatomy/physiology biology, extra chemistry, full physics/math set); three template files patched for iPhone Safari compliance. After running the seed script and restarting the service, all new topics appear with content auto-generating via the existing `_bulk_generate()` lifespan hook.
</objective>

<execution_context>
@/home/linda/.claude/get-shit-done/workflows/execute-plan.md
@/home/linda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-coverage-completion-and-mobile-polish/04-RESEARCH.md

<interfaces>
<!-- Key patterns from existing codebase. Executor uses these directly. -->

From seed_topics.py (INSERT OR IGNORE pattern):
```python
{"slug": "cellula-eucariotica", "title_it": "Cellula eucariotica", "title_en": "Eukaryotic cell",
 "subject": "biology", "order_in_subject": 1}
# subject values: "biology" | "chemistry" | "physics" | "logic"
# content_it and content_en are seeded as NULL — _bulk_generate() fills them on restart
```

From seed_topics.py seed() function:
```python
cur.execute(
    """INSERT OR IGNORE INTO topics
       (slug, title_it, title_en, subject, order_in_subject, content_it, content_en, generated_at)
       VALUES (?, ?, ?, ?, ?, NULL, NULL, NULL)""",
    (topic["slug"], topic["title_it"], topic["title_en"],
     topic["subject"], topic["order_in_subject"])
)
```

From app/templates/fragments/explainer_content.html (line 5):
```html
<div class="prose prose-sm max-w-none text-base-content leading-relaxed ...">
```

From app/templates/fragments/chat_panel.html (line 22-26):
```html
<input type="text" id="chat-input" name="question"
       class="input input-bordered input-sm w-full"
       placeholder="Fai una domanda..."
       autocomplete="off"
       style="margin-bottom: 0.5rem;">
```

From app/templates/topic.html (lines 23-36):
```html
<button class="join-item btn btn-sm min-h-[44px] {% if lang == 'it' %}btn-primary{% else %}btn-ghost{% endif %}"
```
<!-- NOTE: min-h-[44px] is a purged Tailwind class — it has NO effect. Must use inline style instead. -->

Service restart command:
```bash
sudo systemctl restart osteoprep
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend seed_topics.py with all missing topics</name>
  <files>seed_topics.py</files>
  <action>
Append new entries to the TOPICS list in seed_topics.py. Add ALL of the following:

**Biology additions (subject: "biology"):**
- mitocondri: slug="mitocondri", title_it="Mitocondri e respirazione cellulare", title_en="Mitochondria and cellular respiration", order_in_subject=11
  (This fixes the orphan: 3 quiz questions already exist with topic_slug="mitocondri" but no topics row)
- tessuti-animali: slug="tessuti-animali", title_it="Tessuti animali", title_en="Animal tissues", order_in_subject=12
- sistemi-organo: slug="sistemi-organo", title_it="Sistemi d'organo umani", title_en="Human organ systems", order_in_subject=13
- virus-procarioti: slug="virus-procarioti", title_it="Virus e procarioti", title_en="Viruses and prokaryotes", order_in_subject=14
- biotecnologie-dna: slug="biotecnologie-dna", title_it="Biotecnologie e DNA ricombinante", title_en="Biotechnology and recombinant DNA", order_in_subject=15

**Chemistry additions (subject: "chemistry"):**
- soluzioni-proprieta: slug="soluzioni-proprieta", title_it="Soluzioni e proprietà", title_en="Solutions and properties", order_in_subject=11
- equilibrio-chimico: slug="equilibrio-chimico", title_it="Equilibrio chimico e tamponi", title_en="Chemical equilibrium and buffers", order_in_subject=12
- nomenclatura-inorganica: slug="nomenclatura-inorganica", title_it="Nomenclatura composti inorganici", title_en="Inorganic compound nomenclature", order_in_subject=13
- chimica-organica: slug="chimica-organica", title_it="Chimica organica: idrocarburi e gruppi funzionali", title_en="Organic chemistry: hydrocarbons and functional groups", order_in_subject=14

**Physics/Math (subject: "physics") — NEW SUBJECT:**
- grandezze-fisiche: slug="grandezze-fisiche", title_it="Grandezze fisiche e misura", title_en="Physical quantities and measurement", order_in_subject=1
- cinematica: slug="cinematica", title_it="Cinematica", title_en="Kinematics", order_in_subject=2
- dinamica: slug="dinamica", title_it="Dinamica e le leggi di Newton", title_en="Dynamics and Newton's laws", order_in_subject=3
- meccanica-fluidi: slug="meccanica-fluidi", title_it="Meccanica dei fluidi", title_en="Fluid mechanics", order_in_subject=4
- termodinamica: slug="termodinamica", title_it="Termodinamica", title_en="Thermodynamics", order_in_subject=5
- elettromagnetismo: slug="elettromagnetismo", title_it="Elettromagnetismo", title_en="Electromagnetism", order_in_subject=6
- algebra-aritmetica: slug="algebra-aritmetica", title_it="Algebra e aritmetica", title_en="Algebra and arithmetic", order_in_subject=7
- funzioni: slug="funzioni", title_it="Funzioni matematiche", title_en="Mathematical functions", order_in_subject=8
- geometria: slug="geometria", title_it="Geometria e trigonometria", title_en="Geometry and trigonometry", order_in_subject=9
- probabilita-statistica: slug="probabilita-statistica", title_it="Probabilità e statistica", title_en="Probability and statistics", order_in_subject=10

After editing the file, run the seed script from the project root (NOT the venv — script uses stdlib sqlite3 only):
```bash
cd /home/linda/projects/osteoprep && python seed_topics.py
```

Verify output shows > 0 inserted (expect ~23 new rows).

Then restart the service so _bulk_generate() kicks off explainer generation for all new NULL-content topics:
```bash
sudo systemctl restart osteoprep
```

Wait 5 seconds for startup, then verify the service is running:
```bash
sudo systemctl is-active osteoprep
```
  </action>
  <verify>
    <automated>cd /home/linda/projects/osteoprep && python -c "import sqlite3; conn = sqlite3.connect('data/osteoprep.db'); rows = conn.execute('SELECT COUNT(*) FROM topics WHERE subject=?', ('physics',)).fetchone()[0]; bio = conn.execute('SELECT COUNT(*) FROM topics WHERE subject=?', ('biology',)).fetchone()[0]; print(f'physics={rows}, biology={bio}'); conn.close(); assert rows >= 10, f'Expected >=10 physics topics, got {rows}'; assert bio >= 14, f'Expected >=14 biology topics, got {bio}'; print('OK')"</automated>
  </verify>
  <done>At least 10 physics topics and at least 14 biology topics exist in the DB. The "mitocondri" slug has a topics row. The service is active.</done>
</task>

<task type="auto">
  <name>Task 2: Fix three iPhone Safari mobile polish issues in templates</name>
  <files>app/templates/fragments/explainer_content.html, app/templates/fragments/chat_panel.html, app/templates/topic.html</files>
  <action>
Apply three targeted inline-style fixes. DaisyUI is pre-compiled — do NOT add Tailwind utility classes that aren't already in the CSS. Use inline `style="..."` for all sizing.

**Fix 1 — explainer_content.html: prose body text below 16px**
Current line 5:
```html
<div class="prose prose-sm max-w-none text-base-content leading-relaxed
```
Change to (remove `-sm` from prose class, add inline font-size override):
```html
<div class="prose max-w-none text-base-content leading-relaxed
            prose-headings:text-base-content prose-headings:font-bold
            prose-h2:text-base prose-h2:mt-5 prose-h2:mb-2 prose-h2:border-b prose-h2:border-base-300 prose-h2:pb-1
            prose-li:my-0.5 prose-ul:my-1 prose-ol:my-1"
     style="font-size: 1rem;">
```

**Fix 2 — chat_panel.html: chat input under 44px tap target**
Current: `class="input input-bordered input-sm w-full"` with no min-height style.
Change to remove `input-sm` and add inline min-height:
```html
<input type="text" id="chat-input" name="question"
       class="input input-bordered w-full"
       placeholder="Fai una domanda..."
       autocomplete="off"
       style="margin-bottom: 0.5rem; min-height: 44px;">
```

**Fix 3 — topic.html: language toggle buttons — min-h-[44px] is a purged class that has no effect**
Find the two IT and EN toggle buttons (lines 22-38). Both have `class="join-item btn btn-sm min-h-[44px] ..."`.
Add `style="min-height: 44px;"` as an inline attribute alongside the existing class on BOTH buttons. Keep all existing classes unchanged — only add the style attribute.

Result for IT button:
```html
<button
  class="join-item btn btn-sm min-h-[44px] {% if lang == 'it' %}btn-primary{% else %}btn-ghost{% endif %}"
  style="min-height: 44px;"
  hx-get="/topic/{{ topic.slug }}/content?lang=it"
  ...>
  IT
</button>
```

After making all three edits, verify the files are correct with a quick grep to confirm the changes landed.
  </action>
  <verify>
    <automated>cd /home/linda/projects/osteoprep && grep -c "font-size: 1rem" app/templates/fragments/explainer_content.html && grep -c "min-height: 44px" app/templates/fragments/chat_panel.html && grep -c 'style="min-height: 44px;"' app/templates/topic.html</automated>
  </verify>
  <done>explainer_content.html has `font-size: 1rem` inline style; chat_panel.html chat input has `min-height: 44px` in its style attribute; topic.html language toggle buttons both have `style="min-height: 44px;"`. Three files changed, zero Tailwind utility classes added.</done>
</task>

</tasks>

<verification>
Run full verification after both tasks complete:

```bash
cd /home/linda/projects/osteoprep

# 1. Topic counts
python -c "
import sqlite3
conn = sqlite3.connect('data/osteoprep.db')
for subj in ['biology','chemistry','physics']:
    n = conn.execute('SELECT COUNT(*) FROM topics WHERE subject=?',(subj,)).fetchone()[0]
    print(f'{subj}: {n} topics')
mito = conn.execute(\"SELECT slug FROM topics WHERE slug='mitocondri'\").fetchone()
print(f'mitocondri row: {mito}')
conn.close()
"

# 2. Service is running
sudo systemctl is-active osteoprep

# 3. Mobile fixes in place
grep "font-size: 1rem" app/templates/fragments/explainer_content.html
grep "min-height: 44px" app/templates/fragments/chat_panel.html
grep 'style="min-height: 44px;"' app/templates/topic.html
```
</verification>

<success_criteria>
1. `topics` table has >= 10 physics topics, >= 14 biology topics, >= 14 chemistry topics
2. `mitocondri` topic row exists in DB (the 3 orphan quiz questions now have a valid parent)
3. Service is active (explainer generation for new topics is underway in background)
4. explainer_content.html uses `prose` (not `prose-sm`) and has `style="font-size: 1rem;"`
5. chat_panel.html input has `min-height: 44px` in inline style
6. topic.html both language toggle buttons have `style="min-height: 44px;"` as inline attribute
</success_criteria>

<output>
After completion, create `.planning/phases/04-coverage-completion-and-mobile-polish/04-01-SUMMARY.md`
</output>
